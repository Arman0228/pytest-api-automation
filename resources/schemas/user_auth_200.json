{
  "type": "object",
  "required": ["user_id"],
  "properties": {
    "user_id": {
      "oneOf": [
        {"type": "integer"},
        {"type": "string", "pattern": "^[0-9]+$"}
      ]
    }
  }
}
